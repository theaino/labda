$v = ($valid.valid).
$iv = ($invalid.invalid).

$0 = $f.$x.x.
$1 = $f.$x.f x.


/* Church encoding */
$++ = $n.$f.$x.f (n f x).
$+ = $m.$n.n ++ m.
$* = $m.$n.$f.m (n f).
$^ = $m.$n.n m.

$-- = $n.$f.$x.n ($g.$h.h (g f)) ($u.x) ($u.u).
$- = $m.$n.n -- m. /* Monus */

++ 1

/*
$K = $x.$y.x.
$Y = $f.($x.f (x x))($x.f (x x)).

Y (K v)
*/
